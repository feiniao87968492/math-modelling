# Protocol — Fallback and Deviation

## Core Rule

Tool fallback may happen automatically, but method deviation must never happen silently.

If fallback changes the confirmed method, model structure, evidence path, or claim level, create a new blocking decision document before continuing.
